#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int T;
  cin >> T;

  while (T--> 0) {

    // N - kiosk
    // M - people
    int N, M;
    cin >> N >> M;

    vector<int> time(N);
    for (int i = 0; i < N; ++i) {
      cin >> time[i];
    }

    int p = 0;
    int t = 0;

    while (p < M) {
    }


  }
  return 0;
}
